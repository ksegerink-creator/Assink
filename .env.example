# Production site URL — used for canonical URLs, hreflang, sitemap and Open Graph.
SITE_URL="https://www.assinkschipholt.nl"

# Endpoint that receives contact- and offerte-form submissions.
# The forms POST here (see src/components/forms/*). Configure a mail/CRM handler
# (e.g. a serverless function or form service) before going live.
PUBLIC_FORM_ENDPOINT=""
